import data from '/assets/js/data.js';
let easy = getElementById(alignDifficulty).easy;
let hard = getElementById(alignDifficulty).Hard;
let checkbox = getElementById(labelCheckbox).value;
let button = getElementById(proceedButton).value;
function difficulty_selector(easy,hard) {
    if (easy==checkbox){
        addEventListener
    }
    function addEventListener<checkbox>(type: click) => console.log(ciao),
}
console.log(data);